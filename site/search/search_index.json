{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"\ud83d\udccb Note: This is the project landing page, not the full documentation site. For complete guides, CLI reference, Python API docs, and real-world examples, use the Full Documentation button below. \ud83e\uddca VoxelKit <p>Inspect, preview, and QA-check multidimensional imaging data from one unified Python and CLI workflow.</p> \ud83d\udcd6 Full Documentation \u2192 \u2b50 Star on GitHub <p>\u26a1 Get started in seconds</p> <code>pip install voxelkit</code> \ud83d\udccb Copy <p>     Requires Python \u2265 3.9 \u00a0\u00b7\u00a0 MIT License \u00a0\u00b7\u00a0     Extras: <code>pip install voxelkit[gui]</code> for Streamlit GUI \u00a0\u00b7\u00a0     100% local \u2014 no cloud uploads   </p> \u2728 Features <p>Everything you need for day-to-day imaging dataset triage</p> \ud83d\udd0d File Inspection <p>Extract shape, dtype, and metadata from any supported format instantly \u2014 no format-specific boilerplate required.</p> \ud83d\uddbc\ufe0f PNG Slice Previews <p>Generate crisp 2D previews from 3D/4D volumes with configurable plane, slice index, and colormap in a single command.</p> \ud83d\udcca Per-File QA Reports <p>Compute statistics and auto-detect problems \u2014 NaNs, Infs, constant arrays, near-zero volumes \u2014 for any single file.</p> \ud83d\udcc1 Batch QA Reporting <p>Aggregate QA across an entire directory with <code>report-batch</code>. Surface dataset-level risks before they reach your pipeline.</p> \ud83e\uddec Embedding Analysis <p>Specialized QA for 2D embedding matrices \u2014 detect dead dimensions, outlier samples, and per-dimension norm statistics.</p> \ud83d\udd0c Unified Python API <p>Four clean functions \u2014 <code>inspect_file</code>, <code>preview_file</code>, <code>report_file</code>, <code>report_batch</code> \u2014 work identically across all formats.</p> \ud83c\udf10 REST API <p>FastAPI-powered HTTP endpoints for remote inspection, preview generation, and QA reporting \u2014 drop into any stack.</p> \ud83d\udda5\ufe0f Optional Local GUI <p>Streamlit-based offline interface for point-and-click workflows. Launch in seconds with <code>voxelkit gui</code>.</p> \ud83d\udcc2 Supported Formats <p>One toolkit, four imaging ecosystems</p> \ud83e\udde0 .nii \u00a0/\u00a0 .nii.gz \ud83d\uddc4\ufe0f .h5 \u00a0/\u00a0 .hdf5 \ud83d\udd22 .npy \u00a0/\u00a0 .npz \ud83d\uddbc\ufe0f .tif \u00a0/\u00a0 .tiff \ud83d\udc65 Who Can Benefit <p>VoxelKit fits wherever multidimensional data needs triage</p> \ud83d\udd2c Neuroscience Researchers <p>Quickly inspect and QA fMRI, DTI, and structural MRI datasets in NIfTI format \u2014 no format-specific boilerplate.</p> \ud83c\udfe5 Medical Imaging Teams <p>Run batch QA across large clinical datasets to catch constant arrays, NaN-contaminated volumes, or zero-dominated scans before analysis.</p> \ud83e\udd16 ML / AI Practitioners <p>Validate imaging tensors and embedding matrices before training. Catch dead dimensions and outliers early to prevent silent failures.</p> \ud83d\udcd0 Data Scientists <p>Rapidly explore unfamiliar multidimensional datasets from collaborators without reading format-specific documentation first.</p> \ud83c\udfd7\ufe0f Data Engineers <p>Integrate VoxelKit's REST API or Python interface into automated pipelines to enforce data quality gates at ingestion time.</p> \ud83c\udf0d Geospatial Engineers <p>Inspect and QA multi-band raster stacks, satellite imagery tiles, and elevation models stored as NumPy, HDF5, or TIFF arrays.</p> \ud83c\udf93 Educators &amp; Students <p>Use the GUI and CLI to interactively explore neuroimaging and scientific datasets in teaching and research settings.</p> Ready to triage your data? <p>The full documentation covers installation, CLI reference, Python API, QA warning types, and real-world examples.</p> \ud83d\udcd6 Read the Full Docs \u2192 \ud83d\udce6 View on PyPI \u2b50 GitHub"},{"location":"cli/","title":"CLI Reference","text":"<p>The CLI documentation has moved to a more detailed section. Head here:</p> <p>CLI Reference \u2192</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>Hey \u2014 welcome. VoxelKit is a toolkit for inspecting, previewing, and QA-checking multidimensional imaging files (NIfTI, HDF5, NumPy, TIFF) without needing to know the details of each format's Python library.</p> <p>This page gets you from zero to your first result in about five minutes.</p>"},{"location":"getting-started/#install","title":"Install","text":"<pre><code>pip install voxelkit\n</code></pre> <p>That covers everything \u2014 the Python library, the CLI, and the REST API server. The only thing not included by default is the optional local GUI:</p> <pre><code>pip install voxelkit[gui]\n</code></pre> <p>Python 3.9 or later is required.</p> Installing from source (for contributors) <pre><code>git clone https://github.com/ArsalaanAhmad/VoxelKit.git\ncd VoxelKit\npip install -e .\n</code></pre>"},{"location":"getting-started/#your-first-three-commands","title":"Your first three commands","text":"<p>Once installed, the <code>voxelkit</code> command is available in your terminal. Try these on any file you have:</p> <pre><code>voxelkit inspect yourfile.nii.gz      # What's in this file?\nvoxelkit report  yourfile.h5          # Is the data healthy?\nvoxelkit preview yourfile.npy --output preview.png   # Show me a slice\n</code></pre> <p>VoxelKit figures out the format from the file extension \u2014 no flags needed for that.</p>"},{"location":"getting-started/#dont-have-a-file-generate-the-test-fixtures","title":"Don't have a file? Generate the test fixtures","text":"<p>The repo ships with a script that creates small sample files for every supported format:</p> <pre><code>python tests/create_fixtures.py\n</code></pre> <p>Then run against them:</p> <pre><code>voxelkit inspect tests/fixtures/sample_3d.nii.gz\nvoxelkit report  tests/fixtures/sample_nested.h5 --dataset data/subject01/run1/bold\nvoxelkit preview tests/fixtures/sample_3d.nii.gz --plane axial --slice 4 --output out.png\n</code></pre>"},{"location":"getting-started/#what-to-read-next","title":"What to read next","text":"<p>Pick the interface that fits your workflow:</p> <ul> <li> <p> Library Reference</p> <p>Use VoxelKit directly in Python code \u2014 four functions, one import.</p> </li> <li> <p> CLI Reference</p> <p>Run everything from the terminal. Good for quick one-off checks.</p> </li> <li> <p> REST API</p> <p>Start a local HTTP server and call VoxelKit over HTTP from any stack.</p> </li> </ul>"},{"location":"python-usage/","title":"Python Usage","text":"<p>The library documentation has moved to a more detailed section. Head here:</p> <p>Library Reference \u2192</p>"},{"location":"qa-warnings/","title":"QA Warnings","text":"<p>Whenever <code>report_file</code>, <code>report_batch</code>, or <code>voxelkit report</code> runs, it checks for known data quality issues and appends any problems to the <code>warnings</code> list. An empty list means everything looks fine.</p> <p>Here's what each warning means and what you should do when you see it.</p>"},{"location":"qa-warnings/#array-is-constant-or-nearly-constant","title":"Array is constant or nearly constant","text":"<p>What it means: All values in the array are the same (or nearly the same). There's no signal variation in the data.</p> <p>What causes it: A failed scan, a zero-padded placeholder, a corrupted export, or a preprocessing bug that flattened the values.</p> <p>What to do: Double-check the source file. If it's supposed to have variation (e.g. an fMRI BOLD signal), this is a problem. If it's an intentional mask or label file, it may be expected.</p>"},{"location":"qa-warnings/#array-is-mostly-zeros","title":"Array is mostly zeros","text":"<p>What it means: More than 95% of the array's values are exactly zero.</p> <p>What causes it: Over-aggressive masking, a failed normalization step, or the array was never filled properly.</p> <p>What to do: Check whether the zero regions are intentional (brain mask, background). If you expected real signal, investigate the pipeline step that produced the file.</p>"},{"location":"qa-warnings/#contains-nans","title":"Contains NaNs","text":"<p>What it means: The array contains one or more <code>NaN</code> (not-a-number) values.</p> <p>What causes it: Division by zero during preprocessing, failed interpolation, or loading from a corrupted source.</p> <p>What to do: This is almost always a bug. NaNs propagate silently through computations \u2014 a single NaN can corrupt a model loss or a summary statistic. Track down where it was introduced and fix it before using the data.</p>"},{"location":"qa-warnings/#contains-infs","title":"Contains Infs","text":"<p>What it means: The array contains one or more <code>Inf</code> (infinite) values.</p> <p>What causes it: Same causes as NaNs \u2014 overflow, division by near-zero values, or a failed normalization.</p> <p>What to do: Same advice as NaNs. Infs are just as dangerous and harder to spot in summary statistics because they affect the mean and std dramatically.</p>"},{"location":"qa-warnings/#unsupported-dimensionality-for-preview","title":"Unsupported dimensionality for preview","text":"<p>What it means: VoxelKit can't generate a 2D preview of this array because its dimensionality isn't supported (e.g. a 1D or 5D array).</p> <p>What to do: This only affects <code>preview_file</code> / <code>voxelkit preview</code>. The inspection and report functions will still work fine. If you need a visual, extract a slice manually before calling preview.</p>"},{"location":"qa-warnings/#embedding-specific-warnings","title":"Embedding-specific warnings","text":"<p>These appear in <code>report_embedding</code> / <code>voxelkit embed-report</code> output only.</p>"},{"location":"qa-warnings/#n-dimensions-are-dead-std-0","title":"N dimensions are dead (std \u2248 0)","text":"<p>Some embedding dimensions carry no signal \u2014 their values are identical across all samples. A warning fires when more than 5% of dimensions are dead.</p> <p>What causes it: A collapsed layer in the model, a bug in the feature extraction pipeline, or an undertrained embedding space.</p> <p>What to do: Investigate the model or pipeline that produced the embeddings. Dead dimensions are wasted capacity and can cause instability in downstream distance computations.</p>"},{"location":"qa-warnings/#n-dimensions-contain-nan-inf","title":"N dimension(s) contain NaN / Inf","text":"<p>One or more columns have non-finite values that will corrupt any dot product or cosine similarity that uses them.</p> <p>What to do: Same as the global NaN/Inf warnings above \u2014 this is a bug, not a data property.</p>"},{"location":"qa-warnings/#n-samples-have-anomalous-l2-norm-3-from-mean","title":"N sample(s) have anomalous L2 norm (&gt;3\u03c3 from mean)","text":"<p>Some samples have embedding vectors whose length is more than 3 standard deviations from the mean. These are likely corrupted or out-of-distribution.</p> <p>What to do: Inspect the flagged samples. They may need to be removed before training or retrieval.</p>"},{"location":"qa-warnings/#all-sample-l2-norms-are-identical","title":"All sample L2 norms are identical","text":"<p>Every sample has the same L2 norm \u2014 the embedding space has collapsed to a single point. No two samples are distinguishable.</p> <p>What to do: Your model almost certainly has a bug. This is a hard failure, not a minor warning.</p>"},{"location":"api/","title":"REST API","text":"<p>VoxelKit ships with a FastAPI server. It exposes the same inspect, preview, and report operations as the CLI and Python library \u2014 but over HTTP, so you can call them from any language or service.</p>"},{"location":"api/#starting-the-server","title":"Starting the server","text":"<pre><code>uvicorn app.main:app --reload\n</code></pre> <p>The API will be live at <code>http://127.0.0.1:8000</code>.</p> <p>Interactive docs</p> <p>FastAPI generates Swagger UI automatically. Open <code>http://127.0.0.1:8000/docs</code> in your browser to try every endpoint interactively without writing any code.</p>"},{"location":"api/#health-check","title":"Health check","text":"<pre><code>GET /health\n</code></pre> <p>Returns <code>{\"status\": \"ok\"}</code> if the server is running. Useful as a liveness probe.</p> <pre><code>curl http://127.0.0.1:8000/health\n# {\"status\":\"ok\"}\n</code></pre>"},{"location":"api/#endpoint-overview","title":"Endpoint overview","text":"<p>All endpoints accept <code>multipart/form-data</code> file uploads. JSON endpoints return <code>application/json</code>. Preview endpoints return <code>image/png</code>.</p> Endpoint Method Returns Page <code>/nifti/metadata</code> POST JSON metadata \u2192 <code>/nifti/preview</code> POST PNG image \u2192 <code>/h5/inspect</code> POST JSON metadata \u2192 <code>/h5/slice</code> POST PNG image \u2192 <code>/tiff/metadata</code> POST JSON metadata \u2192 <code>/tiff/preview</code> POST PNG image \u2192 <code>/tiff/report</code> POST JSON QA report \u2192 <code>/embedding/report</code> POST JSON QA report \u2192 <code>/embedding/preview</code> POST PNG heatmap \u2192 <code>/health</code> GET <code>{\"status\":\"ok\"}</code> above"},{"location":"api/#general-request-pattern","title":"General request pattern","text":"<p>Every endpoint takes a file upload. For JSON responses:</p> <pre><code>curl -X POST http://127.0.0.1:8000/nifti/metadata \\\n  -F \"file=@scan.nii.gz\"\n</code></pre> <p>For PNG responses, save to a file:</p> <pre><code>curl -X POST \"http://127.0.0.1:8000/nifti/preview?plane=axial\" \\\n  -F \"file=@scan.nii.gz\" \\\n  --output preview.png\n</code></pre>"},{"location":"api/#error-responses","title":"Error responses","text":"Status Meaning <code>400</code> Bad request \u2014 wrong file type, missing required parameter, invalid input <code>500</code> Server error \u2014 the file could not be processed"},{"location":"api/embedding/","title":"Embedding endpoints","text":"<p>Two endpoints for 2D embedding matrices (<code>.npy</code> files with shape <code>(N_samples, D_dims)</code>).</p>"},{"location":"api/embedding/#post-embeddingreport","title":"POST /embedding/report","text":"<p>Generate an embedding-aware QA report for an uploaded <code>.npy</code> matrix.</p> <p>Request: <code>multipart/form-data</code></p> Field Type Description <code>file</code> file A <code>.npy</code> file containing a 2D float array <p>Response: <code>application/json</code></p> <pre><code>{\n  \"filename\": \"features.npy\",\n  \"format\": \"numpy\",\n  \"n_samples\": 10000,\n  \"n_dims\": 512,\n  \"dtype\": \"float32\",\n  \"total_nan_count\": 0,\n  \"total_inf_count\": 0,\n  \"dead_dim_count\": 3,\n  \"nan_dim_count\": 0,\n  \"inf_dim_count\": 0,\n  \"norm_mean\": 14.2,\n  \"norm_std\": 1.8,\n  \"outlier_sample_count\": 12,\n  \"warnings\": [\n    \"3/512 dimensions are dead (std \u2248 0).\",\n    \"12 sample(s) have anomalous L2 norm (&gt;3\u03c3 from mean).\"\n  ]\n}\n</code></pre> <p>Example:</p> <pre><code>curl -X POST http://127.0.0.1:8000/embedding/report \\\n  -F \"file=@features.npy\"\n</code></pre> <pre><code>import httpx\n\nwith open(\"features.npy\", \"rb\") as f:\n    response = httpx.post(\n        \"http://127.0.0.1:8000/embedding/report\",\n        files={\"file\": f},\n    )\n\nreport = response.json()\nprint(f\"Dead dims: {report['dead_dim_count']}\")\nprint(f\"Outliers: {report['outlier_sample_count']}\")\n</code></pre>"},{"location":"api/embedding/#post-embeddingpreview","title":"POST /embedding/preview","text":"<p>Render an uploaded <code>.npy</code> embedding matrix as a per-column-normalised PNG heatmap.</p> <p>Each pixel row is one sample, each pixel column is one embedding dimension. Dead dimensions appear as uniform mid-grey stripes. Large matrices are randomly subsampled.</p> <p>Request: <code>multipart/form-data</code> + query parameters</p> Field Type Description <code>file</code> file A <code>.npy</code> file containing a 2D float array Query param Type Required Default Description <code>max_samples</code> integer No <code>256</code> Maximum rows to render. Large matrices are randomly subsampled <p>Response: <code>image/png</code></p> <p>Example:</p> <pre><code>curl -X POST \"http://127.0.0.1:8000/embedding/preview?max_samples=512\" \\\n  -F \"file=@features.npy\" \\\n  --output heatmap.png\n</code></pre> <pre><code>import httpx\n\nwith open(\"features.npy\", \"rb\") as f:\n    response = httpx.post(\n        \"http://127.0.0.1:8000/embedding/preview\",\n        params={\"max_samples\": 512},\n        files={\"file\": f},\n    )\n\nwith open(\"heatmap.png\", \"wb\") as out:\n    out.write(response.content)\n</code></pre>"},{"location":"api/h5/","title":"HDF5 endpoints","text":"<p>Two endpoints for HDF5 files (<code>.h5</code>, <code>.hdf5</code>).</p>"},{"location":"api/h5/#post-h5inspect","title":"POST /h5/inspect","text":"<p>Inspect an uploaded HDF5 file \u2014 returns the list of all datasets with their shapes and dtypes.</p> <p>Request: <code>multipart/form-data</code></p> Field Type Description <code>file</code> file A <code>.h5</code> or <code>.hdf5</code> file <p>Response: <code>application/json</code></p> <pre><code>{\n  \"filename\": \"experiment.h5\",\n  \"format\": \"hdf5\",\n  \"datasets\": [\n    {\n      \"path\": \"data/subject01/run1/bold\",\n      \"shape\": [64, 64, 30, 200],\n      \"dtype\": \"float32\"\n    },\n    {\n      \"path\": \"data/subject02/run1/bold\",\n      \"shape\": [64, 64, 30, 198],\n      \"dtype\": \"float32\"\n    }\n  ]\n}\n</code></pre> <p>Example:</p> <pre><code>curl -X POST http://127.0.0.1:8000/h5/inspect \\\n  -F \"file=@experiment.h5\"\n</code></pre> <pre><code>import httpx\n\nwith open(\"experiment.h5\", \"rb\") as f:\n    response = httpx.post(\n        \"http://127.0.0.1:8000/h5/inspect\",\n        files={\"file\": f},\n    )\n\nfor ds in response.json()[\"datasets\"]:\n    print(ds[\"path\"], ds[\"shape\"])\n</code></pre>"},{"location":"api/h5/#post-h5slice","title":"POST /h5/slice","text":"<p>Generate a PNG slice preview from a dataset inside an uploaded HDF5 file.</p> <p>Request: <code>multipart/form-data</code> + query parameters</p> Field Type Description <code>file</code> file A <code>.h5</code> or <code>.hdf5</code> file Query param Type Required Description <code>dataset_path</code> string Yes Path to the dataset inside the file, e.g. <code>data/subject01/run1/bold</code> <code>axis</code> integer Yes Axis to slice along (0, 1, or 2) <code>slice_index</code> integer No Slice index; defaults to centre <p>Response: <code>image/png</code></p> <p>Example:</p> <pre><code>curl -X POST \\\n  \"http://127.0.0.1:8000/h5/slice?dataset_path=data/subject01/run1/bold&amp;axis=2&amp;slice_index=10\" \\\n  -F \"file=@experiment.h5\" \\\n  --output slice.png\n</code></pre> <pre><code>import httpx\n\nwith open(\"experiment.h5\", \"rb\") as f:\n    response = httpx.post(\n        \"http://127.0.0.1:8000/h5/slice\",\n        params={\n            \"dataset_path\": \"data/subject01/run1/bold\",\n            \"axis\": 2,\n            \"slice_index\": 10,\n        },\n        files={\"file\": f},\n    )\n\nwith open(\"slice.png\", \"wb\") as out:\n    out.write(response.content)\n</code></pre>"},{"location":"api/nifti/","title":"NIfTI endpoints","text":"<p>Two endpoints for NIfTI files (<code>.nii</code>, <code>.nii.gz</code>).</p>"},{"location":"api/nifti/#post-niftimetadata","title":"POST /nifti/metadata","text":"<p>Extract metadata from an uploaded NIfTI file.</p> <p>Request: <code>multipart/form-data</code></p> Field Type Description <code>file</code> file A <code>.nii</code> or <code>.nii.gz</code> file <p>Response: <code>application/json</code></p> <pre><code>{\n  \"filename\": \"scan.nii.gz\",\n  \"format\": \"nifti\",\n  \"shape\": [64, 64, 30],\n  \"voxel_sizes\": [3.0, 3.0, 4.0],\n  \"data_dtype\": \"float32\",\n  \"affine\": [[...], ...],\n  \"header\": { \"...\": \"...\" }\n}\n</code></pre> <p>Example:</p> <pre><code>curl -X POST http://127.0.0.1:8000/nifti/metadata \\\n  -F \"file=@bold.nii.gz\"\n</code></pre> <pre><code>import httpx\n\nwith open(\"bold.nii.gz\", \"rb\") as f:\n    response = httpx.post(\n        \"http://127.0.0.1:8000/nifti/metadata\",\n        files={\"file\": f},\n    )\nprint(response.json()[\"shape\"])\n</code></pre>"},{"location":"api/nifti/#post-niftipreview","title":"POST /nifti/preview","text":"<p>Generate a PNG slice preview from an uploaded NIfTI file.</p> <p>Request: <code>multipart/form-data</code> + query parameters</p> Field Type Description <code>file</code> file A <code>.nii</code> or <code>.nii.gz</code> file Query param Type Required Description <code>plane</code> string Yes One of <code>axial</code>, <code>coronal</code>, <code>sagittal</code> <code>slice_index</code> integer No Slice index; defaults to centre <p>Response: <code>image/png</code></p> <p>Example:</p> <pre><code>curl -X POST \"http://127.0.0.1:8000/nifti/preview?plane=axial&amp;slice_index=10\" \\\n  -F \"file=@bold.nii.gz\" \\\n  --output preview.png\n</code></pre> <pre><code>import httpx\n\nwith open(\"bold.nii.gz\", \"rb\") as f:\n    response = httpx.post(\n        \"http://127.0.0.1:8000/nifti/preview\",\n        params={\"plane\": \"axial\", \"slice_index\": 10},\n        files={\"file\": f},\n    )\n\nwith open(\"preview.png\", \"wb\") as out:\n    out.write(response.content)\n</code></pre>"},{"location":"api/tiff/","title":"TIFF endpoints","text":"<p>Three endpoints for TIFF files (<code>.tif</code>, <code>.tiff</code>).</p>"},{"location":"api/tiff/#post-tiffmetadata","title":"POST /tiff/metadata","text":"<p>Extract metadata from an uploaded TIFF file. Only the file header is read \u2014 no pixel data is loaded.</p> <p>Request: <code>multipart/form-data</code></p> Field Type Description <code>file</code> file A <code>.tif</code> or <code>.tiff</code> file <p>Response: <code>application/json</code></p> <pre><code>{\n  \"filename\": \"volume.tiff\",\n  \"format\": \"tiff\",\n  \"shape\": [50, 512, 512],\n  \"ndim\": 3,\n  \"dtype\": \"uint16\",\n  \"page_count\": 50,\n  \"axes\": \"ZYX\"\n}\n</code></pre> <p>Example:</p> <pre><code>curl -X POST http://127.0.0.1:8000/tiff/metadata \\\n  -F \"file=@volume.tiff\"\n</code></pre>"},{"location":"api/tiff/#post-tiffpreview","title":"POST /tiff/preview","text":"<p>Generate a PNG slice from an uploaded TIFF file. For 2D TIFFs the full image is returned. For 3D z-stacks a single plane is extracted.</p> <p>Request: <code>multipart/form-data</code> + query parameters</p> Field Type Description <code>file</code> file A <code>.tif</code> or <code>.tiff</code> file Query param Type Required Default Description <code>axis</code> integer No <code>0</code> Slice axis for 3D z-stacks (0=Z, 1=Y, 2=X) <code>slice_index</code> integer No centre Slice index <p>Response: <code>image/png</code></p> <p>Example:</p> <pre><code>curl -X POST \"http://127.0.0.1:8000/tiff/preview?axis=0&amp;slice_index=25\" \\\n  -F \"file=@volume.tiff\" \\\n  --output tiff_preview.png\n</code></pre>"},{"location":"api/tiff/#post-tiffreport","title":"POST /tiff/report","text":"<p>Generate a QA report for an uploaded TIFF file.</p> <p>Request: <code>multipart/form-data</code></p> Field Type Description <code>file</code> file A <code>.tif</code> or <code>.tiff</code> file <p>Response: <code>application/json</code></p> <pre><code>{\n  \"filename\": \"volume.tiff\",\n  \"format\": \"tiff\",\n  \"shape\": [50, 512, 512],\n  \"dtype\": \"uint16\",\n  \"min\": 0,\n  \"max\": 65410,\n  \"mean\": 12043.2,\n  \"std\": 8821.5,\n  \"nan_count\": 0,\n  \"inf_count\": 0,\n  \"zero_fraction\": 0.08,\n  \"warnings\": []\n}\n</code></pre> <p>Example:</p> <pre><code>curl -X POST http://127.0.0.1:8000/tiff/report \\\n  -F \"file=@volume.tiff\"\n</code></pre>"},{"location":"cli/","title":"CLI Reference","text":"<p>The <code>voxelkit</code> command gives you everything in the library without writing a line of Python.</p> <pre><code>voxelkit --help\n</code></pre> <p>There are seven commands total:</p> Command What it does <code>inspect</code> Print file metadata as JSON <code>preview</code> Save a PNG slice to disk <code>report</code> Print a QA report as JSON <code>report-batch</code> QA report for every file in a directory <code>embed-report</code> Per-dimension + per-sample embedding QA <code>embed-preview</code> Render an embedding matrix as a heatmap PNG <code>gui</code> Launch the local Streamlit interface"},{"location":"cli/#quick-orientation","title":"Quick orientation","text":"<p>Every command that outputs data prints JSON to stdout. You can pipe it, save it, or format it however you like:</p> <pre><code># pipe to jq for pretty inspection\nvoxelkit inspect myfile.nii.gz | jq '.shape'\n\n# save to a file\nvoxelkit report myfile.h5 &gt; report.json\n</code></pre> <p>Commands that generate images always require <code>--output</code>:</p> <pre><code>voxelkit preview myfile.nii.gz --plane axial --output preview.png\n</code></pre>"},{"location":"cli/#global-help","title":"Global help","text":"<pre><code>voxelkit --help            # top-level help\nvoxelkit inspect --help    # help for a specific command\n</code></pre> <p>Use the sidebar to jump to any individual command.</p>"},{"location":"cli/batch/","title":"voxelkit report-batch","text":"<pre><code>voxelkit report-batch &lt;directory&gt;\n</code></pre> <p>Scans a directory, finds every supported file, runs a QA report on each one, and prints the combined results as a JSON array. This is the go-to command when you need a health check across an entire dataset.</p>"},{"location":"cli/batch/#usage","title":"Usage","text":"<pre><code>voxelkit report-batch DIRECTORY [options]\n</code></pre>"},{"location":"cli/batch/#arguments","title":"Arguments","text":"Argument Description <code>DIRECTORY</code> Path to a directory to scan"},{"location":"cli/batch/#flags","title":"Flags","text":"Flag Description <code>--output PATH</code> Write the JSON output to a file instead of stdout <code>--no-recursive</code> Only scan the top-level directory, don't descend into subdirectories <p>By default, the scan is recursive \u2014 it finds files in subdirectories too.</p>"},{"location":"cli/batch/#examples","title":"Examples","text":"<pre><code># print results to stdout\nvoxelkit report-batch data/study_01/\n\n# save to a file\nvoxelkit report-batch data/study_01/ --output batch_report.json\n\n# top-level only\nvoxelkit report-batch data/study_01/ --no-recursive\n</code></pre>"},{"location":"cli/batch/#output-format","title":"Output format","text":"<p>A JSON array \u2014 one report object per file:</p> <pre><code>[\n  {\n    \"filename\": \"subject01_bold.nii.gz\",\n    \"format\": \"nifti\",\n    \"shape\": [64, 64, 30, 200],\n    \"warnings\": []\n  },\n  {\n    \"filename\": \"subject02_bold.nii.gz\",\n    \"format\": \"nifti\",\n    \"shape\": [64, 64, 30, 200],\n    \"warnings\": [\"Array is mostly zeros.\"]\n  }\n]\n</code></pre>"},{"location":"cli/batch/#find-all-flagged-files-at-once","title":"Find all flagged files at once","text":"<pre><code>voxelkit report-batch data/ --output batch.json\njq '[.[] | select(.warnings | length &gt; 0) | .filename]' batch.json\n</code></pre> <p>Supported extensions</p> <p>VoxelKit picks up <code>.nii</code>, <code>.nii.gz</code>, <code>.h5</code>, <code>.hdf5</code>, <code>.npy</code>, <code>.npz</code>, <code>.tif</code>, and <code>.tiff</code>. Everything else is skipped silently.</p>"},{"location":"cli/embed-preview/","title":"voxelkit embed-preview","text":"<pre><code>voxelkit embed-preview features.npy --output heatmap.png\n</code></pre> <p>Renders your embedding matrix as a PNG heatmap and saves it to disk. Each row is one sample, each column is one dimension. Dead dimensions (no variance) show up as flat mid-grey stripes \u2014 they're immediately obvious in the image.</p>"},{"location":"cli/embed-preview/#usage","title":"Usage","text":"<pre><code>voxelkit embed-preview FILE --output OUTPUT [options]\n</code></pre>"},{"location":"cli/embed-preview/#arguments","title":"Arguments","text":"Argument Description <code>FILE</code> Path to a <code>.npy</code> 2D <code>(N_samples, D_dims)</code> float array"},{"location":"cli/embed-preview/#flags","title":"Flags","text":"Flag Default Description <code>--output PATH</code> \u2014 Required. Output <code>.png</code> file path <code>--max-samples INT</code> <code>256</code> Maximum number of sample rows to render. Large matrices are randomly subsampled"},{"location":"cli/embed-preview/#examples","title":"Examples","text":"<pre><code># basic \u2014 renders up to 256 samples\nvoxelkit embed-preview features.npy --output heatmap.png\n\n# render more rows for a denser view\nvoxelkit embed-preview features.npy --max-samples 1024 --output heatmap_full.png\n</code></pre>"},{"location":"cli/embed-preview/#what-the-image-shows","title":"What the image shows","text":"<p>The heatmap is per-column-normalised: each dimension (column) is independently scaled to 0\u2013255, so you can see internal structure in dimensions regardless of their absolute scale.</p> <ul> <li>Bright/dark variation in a column \u2192 that dimension carries signal</li> <li>Flat uniform colour in a column \u2192 dead dimension (no variance)</li> <li>Isolated extremely bright or dark rows \u2192 potential outlier samples</li> </ul> <p>Use this alongside <code>voxelkit embed-report</code> \u2014 the report gives you the numbers, the heatmap gives you the picture.</p>"},{"location":"cli/embed-report/","title":"voxelkit embed-report","text":"<pre><code>voxelkit embed-report features.npy\n</code></pre> <p>Runs embedding-aware QA on a <code>.npy</code> feature matrix and prints the result as JSON. This goes deeper than <code>voxelkit report</code> \u2014 instead of treating the array as a flat collection of numbers, it analyses it as a matrix of sample vectors and catches embedding-specific failures.</p>"},{"location":"cli/embed-report/#usage","title":"Usage","text":"<pre><code>voxelkit embed-report FILE\n</code></pre>"},{"location":"cli/embed-report/#arguments","title":"Arguments","text":"Argument Description <code>FILE</code> Path to a <code>.npy</code> file containing a 2D <code>(N_samples, D_dims)</code> float array <p><code>.npy</code> only</p> <p>Only <code>.npy</code> is supported. If your matrix is inside an <code>.npz</code> archive, extract it first.</p>"},{"location":"cli/embed-report/#example","title":"Example","text":"<pre><code>voxelkit embed-report model_outputs.npy\n</code></pre> <p>Output:</p> <pre><code>{\n  \"filename\": \"model_outputs.npy\",\n  \"format\": \"numpy\",\n  \"n_samples\": 10000,\n  \"n_dims\": 512,\n  \"dtype\": \"float32\",\n  \"total_nan_count\": 0,\n  \"total_inf_count\": 0,\n  \"dead_dim_count\": 3,\n  \"nan_dim_count\": 0,\n  \"inf_dim_count\": 0,\n  \"norm_mean\": 14.2,\n  \"norm_std\": 1.8,\n  \"outlier_sample_count\": 12,\n  \"warnings\": [\n    \"3/512 dimensions are dead (std \u2248 0). This may indicate a collapsed or undertrained embedding space.\",\n    \"12 sample(s) have anomalous L2 norm (&gt;3\u03c3 from mean).\"\n  ]\n}\n</code></pre>"},{"location":"cli/embed-report/#what-gets-checked","title":"What gets checked","text":"<ul> <li>Dead dimensions \u2014 columns with near-zero std (no signal)</li> <li>NaN / Inf dimensions \u2014 columns that would corrupt similarity computations</li> <li>Outlier samples \u2014 rows with L2 norm more than 3\u03c3 from the mean</li> <li>Collapsed embedding space \u2014 all sample norms are identical (model collapsed)</li> </ul> <p>See Embedding Analysis for the full explanation.</p>"},{"location":"cli/gui/","title":"voxelkit gui","text":"<pre><code>voxelkit gui\n</code></pre> <p>Launches a local Streamlit app in your browser \u2014 a point-and-click interface for inspecting, previewing, and reporting on imaging files without any command-line flags.</p>"},{"location":"cli/gui/#requirements","title":"Requirements","text":"<p>The GUI is an optional extra. Install it with:</p> <pre><code>pip install voxelkit[gui]\n</code></pre> <p>If you try to run <code>voxelkit gui</code> without the extra installed, you'll get a clear error message telling you what to install.</p>"},{"location":"cli/gui/#usage","title":"Usage","text":"<pre><code>voxelkit gui\n</code></pre> <p>That's it. Streamlit will open the app in your default browser automatically (usually at <code>http://localhost:8501</code>).</p>"},{"location":"cli/gui/#what-it-does","title":"What it does","text":"<p>The GUI wraps the same library functions as the CLI \u2014 inspect, preview, report \u2014 in a drag-and-drop interface. It's useful for:</p> <ul> <li>Quickly exploring a new dataset without writing any code</li> <li>Sharing a tool with colleagues who aren't comfortable with the CLI</li> <li>Teaching or demo environments</li> </ul> <p>It runs entirely locally \u2014 no files are sent anywhere.</p>"},{"location":"cli/gui/#notes","title":"Notes","text":"<ul> <li>The GUI is a prototype. For production use or scripting, prefer the CLI or Python library.</li> <li>To stop the server, hit <code>Ctrl+C</code> in the terminal where you ran <code>voxelkit gui</code>.</li> </ul>"},{"location":"cli/inspect/","title":"voxelkit inspect","text":"<pre><code>voxelkit inspect &lt;file&gt;\n</code></pre> <p>Prints a JSON snapshot of the file's structure \u2014 shape, dtype, headers \u2014 to stdout. No pixel data is loaded. This is the fastest way to answer \"what is this file?\"</p>"},{"location":"cli/inspect/#usage","title":"Usage","text":"<pre><code>voxelkit inspect FILE\n</code></pre>"},{"location":"cli/inspect/#arguments","title":"Arguments","text":"Argument Description <code>FILE</code> Path to any supported file (<code>.nii</code>, <code>.nii.gz</code>, <code>.h5</code>, <code>.hdf5</code>, <code>.npy</code>, <code>.npz</code>, <code>.tif</code>, <code>.tiff</code>)"},{"location":"cli/inspect/#examples","title":"Examples","text":"<pre><code># NIfTI\nvoxelkit inspect scan.nii.gz\n\n# HDF5 \u2014 lists all datasets inside the file\nvoxelkit inspect experiment.h5\n\n# NumPy\nvoxelkit inspect features.npy\n\n# TIFF\nvoxelkit inspect volume.tiff\n</code></pre> <p>Example output for a NIfTI file:</p> <pre><code>{\n  \"filename\": \"scan.nii.gz\",\n  \"format\": \"nifti\",\n  \"shape\": [64, 64, 30],\n  \"voxel_sizes\": [3.0, 3.0, 4.0],\n  \"data_dtype\": \"float32\"\n}\n</code></pre>"},{"location":"cli/inspect/#tips","title":"Tips","text":"<p>Pipe the output to <code>jq</code> to query specific fields:</p> <pre><code>voxelkit inspect scan.nii.gz | jq '.shape'\n# [64, 64, 30]\n\nvoxelkit inspect experiment.h5 | jq '.datasets[].path'\n# \"data/subject01/run1/bold\"\n# \"data/subject02/run1/bold\"\n</code></pre>"},{"location":"cli/preview/","title":"voxelkit preview","text":"<pre><code>voxelkit preview &lt;file&gt; --output preview.png\n</code></pre> <p>Generates a 2D PNG slice from your file and saves it to disk. The centre slice is used by default. You can control the plane, axis, and slice index with flags.</p>"},{"location":"cli/preview/#usage","title":"Usage","text":"<pre><code>voxelkit preview FILE --output OUTPUT [options]\n</code></pre>"},{"location":"cli/preview/#arguments","title":"Arguments","text":"Argument Description <code>FILE</code> Path to a supported file"},{"location":"cli/preview/#flags","title":"Flags","text":"Flag Format Description <code>--output PATH</code> All Required. Output <code>.png</code> file path <code>--plane PLANE</code> NIfTI only One of <code>axial</code>, <code>coronal</code>, <code>sagittal</code>. Defaults to <code>axial</code> <code>--dataset PATH</code> HDF5 only Dataset path inside the file, e.g. <code>data/subject01/run1/bold</code> <code>--array NAME</code> <code>.npz</code> only Array name inside the archive <code>--axis INT</code> HDF5 / NumPy / TIFF Axis to slice along (0, 1, or 2). Required for 3D HDF5 datasets <code>--slice INT</code> All Slice index. Defaults to the centre slice <p>Format-specific flags</p> <p><code>--plane</code> is NIfTI-only. <code>--dataset</code> is HDF5-only. <code>--array</code> is <code>.npz</code>-only. Passing the wrong flag for a format raises an error.</p>"},{"location":"cli/preview/#examples","title":"Examples","text":"NIfTIHDF5NumPy .npyNumPy .npzTIFF <pre><code># centre axial slice (default)\nvoxelkit preview bold.nii.gz --output preview.png\n\n# specific plane and slice\nvoxelkit preview bold.nii.gz --plane coronal --slice 20 --output coronal.png\n</code></pre> <pre><code># dataset_path and axis required for 3D datasets\nvoxelkit preview experiment.h5 \\\n  --dataset data/subject01/run1/bold \\\n  --axis 2 \\\n  --slice 10 \\\n  --output h5_preview.png\n</code></pre> <pre><code>voxelkit preview volume.npy --axis 0 --slice 5 --output npy_preview.png\n</code></pre> <pre><code>voxelkit preview data.npz --array X --axis 0 --output npz_preview.png\n</code></pre> <pre><code>voxelkit preview zstack.tiff --axis 0 --slice 25 --output tiff_preview.png\n</code></pre>"},{"location":"cli/preview/#output","title":"Output","text":"<p>The PNG is written to the path given by <code>--output</code>. Parent directories are created automatically if they don't exist. The image is grayscale, normalised to 0\u2013255.</p>"},{"location":"cli/report/","title":"voxelkit report","text":"<pre><code>voxelkit report &lt;file&gt;\n</code></pre> <p>Runs a quality check on a single file and prints the result as JSON. You get statistics (min, max, mean, std, NaN count, zero fraction) and a <code>warnings</code> list that flags anything that looks off.</p>"},{"location":"cli/report/#usage","title":"Usage","text":"<pre><code>voxelkit report FILE [options]\n</code></pre>"},{"location":"cli/report/#arguments","title":"Arguments","text":"Argument Description <code>FILE</code> Path to a supported file"},{"location":"cli/report/#flags","title":"Flags","text":"Flag Format Description <code>--dataset PATH</code> HDF5 only Dataset path inside the file. If omitted, the first dataset is used <code>--array NAME</code> <code>.npz</code> only Array name inside the archive"},{"location":"cli/report/#examples","title":"Examples","text":"<pre><code># NIfTI\nvoxelkit report bold.nii.gz\n\n# HDF5 \u2014 specify a dataset\nvoxelkit report experiment.h5 --dataset data/subject01/run1/bold\n\n# NumPy\nvoxelkit report features.npy\n\n# TIFF\nvoxelkit report volume.tiff\n</code></pre> <p>Example output:</p> <pre><code>{\n  \"filename\": \"bold.nii.gz\",\n  \"format\": \"nifti\",\n  \"shape\": [64, 64, 30, 200],\n  \"dtype\": \"float32\",\n  \"min\": -2.1,\n  \"max\": 4102.8,\n  \"mean\": 810.5,\n  \"std\": 398.2,\n  \"nan_count\": 0,\n  \"inf_count\": 0,\n  \"zero_fraction\": 0.03,\n  \"warnings\": []\n}\n</code></pre>"},{"location":"cli/report/#checking-for-warnings","title":"Checking for warnings","text":"<pre><code>voxelkit report subject02.nii.gz | jq '.warnings'\n# [\n#   \"Array is mostly zeros.\"\n# ]\n</code></pre> <p>See QA Warnings for what each warning means and what to do about it.</p>"},{"location":"cli/report/#saving-the-report","title":"Saving the report","text":"<pre><code>voxelkit report bold.nii.gz &gt; report.json\n</code></pre>"},{"location":"contributing/","title":"How to Contribute","text":"<p>First off \u2014 thank you for even thinking about contributing. VoxelKit is open-source and genuinely gets better when people use it, find rough edges, and send fixes.</p> <p>You don't need to add a whole new feature to contribute. Fixing a typo in the docs, adding a missing test, or clarifying an error message are all genuinely useful.</p>"},{"location":"contributing/#getting-set-up-locally","title":"Getting set up locally","text":"<pre><code>git clone https://github.com/ArsalaanAhmad/VoxelKit.git\ncd VoxelKit\npip install -r requirements.txt\n</code></pre> <p>Run the test suite to make sure everything is green before you start:</p> <pre><code>pytest -q\n</code></pre> <p>Generate the test fixtures if you need sample files to work with:</p> <pre><code>python tests/create_fixtures.py\n</code></pre> <p>Start the API server locally:</p> <pre><code>python -m uvicorn app.main:app --reload\n</code></pre>"},{"location":"contributing/#the-workflow","title":"The workflow","text":"<ol> <li>Fork the repo on GitHub</li> <li>Create a branch from <code>main</code> \u2014 give it a name that describes what you're doing (<code>fix/nan-warning-message</code>, <code>feat/zarr-support</code>)</li> <li>Make your changes \u2014 keep them focused. One thing per PR</li> <li>Run the tests \u2014 add or update tests if you changed behaviour</li> <li>Open a pull request with a short summary of what you changed and why</li> </ol> <p>That's it. No CLA, no hoops.</p>"},{"location":"contributing/#what-makes-a-good-pr","title":"What makes a good PR","text":"<ul> <li>Focused \u2014 one fix or one feature, not five things at once</li> <li>Tested \u2014 if you changed how something behaves, there should be a test that proves it</li> <li>Documented \u2014 if you added a new CLI flag or Python parameter, update the docs</li> <li>Clean commits \u2014 \"fix NaN warning threshold\" is better than \"stuff\"</li> </ul>"},{"location":"contributing/#project-architecture-quick-orientation","title":"Project architecture (quick orientation)","text":"<p>VoxelKit is library-first:</p> <ul> <li><code>voxelkit/&lt;format&gt;/</code> \u2014 the real logic lives here (inspect, preview, report per format)</li> <li><code>voxelkit/cli.py</code> \u2014 thin adapter that routes CLI arguments to library functions</li> <li><code>app/routers/</code> \u2014 thin FastAPI wrappers, same idea as the CLI</li> <li><code>voxelkit/core/</code> \u2014 shared utilities (validation, types, errors, batch reporting)</li> </ul> <p>Adding a new file format?</p> <ol> <li>Add <code>voxelkit/&lt;format&gt;/</code> with <code>inspect.py</code>, <code>preview.py</code>, <code>report.py</code></li> <li>Register it in <code>voxelkit/cli.py</code> via <code>register_format(FormatRoute(...))</code></li> <li>Add a FastAPI router under <code>app/routers/</code> and include it in <code>app/main.py</code></li> <li>Add tests and a fixture file under <code>tests/</code></li> </ol>"},{"location":"contributing/#code-guidelines","title":"Code guidelines","text":"<ul> <li>Small, well-scoped functions</li> <li>Handle invalid input with clear error messages (use <code>ValidationError</code> from <code>voxelkit.core.errors</code>)</li> <li>No unrelated refactors bundled into a feature PR</li> <li>Follow the naming style of the existing code</li> </ul>"},{"location":"contributing/#pull-request-checklist","title":"Pull request checklist","text":"<ul> <li>[ ] Tests added or updated if behaviour changed</li> <li>[ ] Docs updated if you added a new parameter, flag, or endpoint</li> <li>[ ] No unrelated changes bundled in</li> <li>[ ] Commit messages are clear</li> </ul>"},{"location":"contributing/#questions","title":"Questions?","text":"<p>Open an issue \u2014 even just to ask. Reporting Issues \u2192</p>"},{"location":"contributing/issues/","title":"Reporting Issues","text":"<p>Issues are genuinely valuable \u2014 even ones that don't get fixed immediately. They tell me (and anyone who finds the project later) that something is broken or confusing, and they create a paper trail for solutions.</p> <p>If something isn't working, or something is unclear in the docs, please open an issue. You don't need to be certain it's a bug. \"I expected X but got Y\" is enough.</p>"},{"location":"contributing/issues/#where-to-open-one","title":"Where to open one","text":"<p>github.com/ArsalaanAhmad/VoxelKit/issues \u2192 New issue</p>"},{"location":"contributing/issues/#what-to-include","title":"What to include","text":"<p>The more context you give, the faster it gets resolved. Here's what helps:</p> <p>1. What you expected to happen</p> <p>Even one sentence: \"I expected <code>report_file</code> to return a dict with a <code>warnings</code> key.\"</p> <p>2. What actually happened</p> <p>The actual output, error message, or behaviour you got. Copy-paste the exact error if there is one.</p> <p>3. Steps to reproduce</p> <p>The shortest sequence of commands or code that triggers the problem. If you can reduce it to three lines, do that.</p> <p>4. Your environment</p> <pre><code>OS: Windows 11 / macOS 14 / Ubuntu 22.04\nPython version: 3.11.2\nVoxelKit version: 0.1.5  (run: pip show voxelkit)\n</code></pre> <p>5. A sample file (if relevant)</p> <p>If the bug only happens with a specific file, attaching a minimal version of it (or describing its structure) saves a lot of back-and-forth.</p>"},{"location":"contributing/issues/#types-of-issues-worth-opening","title":"Types of issues worth opening","text":"<ul> <li>Bug reports \u2014 something crashes, returns wrong results, or behaves unexpectedly</li> <li>Documentation issues \u2014 something is confusing, missing, or wrong in the docs</li> <li>Feature requests \u2014 a format you'd like supported, a flag you'd find useful</li> <li>Questions \u2014 if you're unsure whether something is a bug or expected behaviour, just ask</li> </ul>"},{"location":"contributing/issues/#you-dont-need-to-be-certain","title":"You don't need to be certain","text":"<p>\"I'm not sure if this is a bug but...\" is a perfectly valid way to start an issue. The worst case is that it turns out to be expected behaviour and gets documented better because of the question. That's still a win.</p>"},{"location":"library/","title":"Library Reference","text":"<p>VoxelKit was built library-first. The CLI and REST API are just thin layers on top of these Python functions \u2014 which means you get the full power of the library when you use it directly in your code.</p> <p>One import is all you need:</p> <pre><code>from voxelkit import inspect_file, preview_file, report_file, report_batch\n</code></pre>"},{"location":"library/#the-four-main-functions","title":"The four main functions","text":"<p>Each function auto-detects the file format from the extension, so you don't have to think about which module handles which format.</p> Function What it returns Page <code>inspect_file(path)</code> Metadata dict \u2014 shape, dtype, headers \u2192 <code>preview_file(path, ...)</code> PNG image as <code>bytes</code> \u2192 <code>report_file(path, ...)</code> QA stats + warnings dict \u2192 <code>report_batch(path, ...)</code> List of reports for a whole directory \u2192"},{"location":"library/#embedding-specific-functions","title":"Embedding-specific functions","text":"<p>If you're working with 2D feature matrices (N samples \u00d7 D dimensions), these two go deeper than the standard <code>report_file</code>:</p> Function What it returns Page <code>report_embedding(path)</code> Per-dimension + per-sample QA \u2192 <code>preview_embedding(path, ...)</code> Heatmap PNG as <code>bytes</code> \u2192"},{"location":"library/#a-note-on-format-specific-imports","title":"A note on format-specific imports","text":"<p>The four main functions cover 95% of use cases. But if you need direct access to a format-specific module \u2014 for example, to call NIfTI inspection directly \u2014 those are also exported:</p> <pre><code>from voxelkit import (\n    inspect_h5, preview_h5, report_h5,\n    inspect_npy, preview_npy, report_npy,\n    nifti_metadata, preview_nifti, report_nifti,\n    inspect_tiff, preview_tiff, report_tiff,\n    report_embedding, preview_embedding,\n)\n</code></pre> <p>For most work, stick with <code>inspect_file</code>, <code>preview_file</code>, <code>report_file</code>, and <code>report_batch</code>.</p>"},{"location":"library/batch/","title":"report_batch","text":"<pre><code>from voxelkit import report_batch\n\nresults = report_batch(\"my_dataset/\")\n</code></pre> <p><code>report_batch</code> scans a directory, finds every supported file inside it, runs <code>report_file</code> on each one, and hands you back the full list. It's the fastest way to get a health check across a whole dataset \u2014 one call, every file, one result.</p>"},{"location":"library/batch/#signature","title":"Signature","text":"<pre><code>def report_batch(\n    path: str | Path,\n    recursive: bool = True,\n) -&gt; list[dict]\n</code></pre>"},{"location":"library/batch/#parameters","title":"Parameters","text":"Parameter Type Default Description <code>path</code> <code>str</code> or <code>Path</code> \u2014 Directory to scan <code>recursive</code> <code>bool</code> <code>True</code> Whether to descend into subdirectories"},{"location":"library/batch/#return-value","title":"Return value","text":"<p>A list of report dictionaries \u2014 one per file found. Each dictionary is the same structure as what <code>report_file</code> returns for that format, plus a <code>warnings</code> list. Files that fail to process are included with an <code>error</code> key instead of stats.</p> <pre><code>[\n  {\n    \"filename\": \"subject01_bold.nii.gz\",\n    \"format\": \"nifti\",\n    \"shape\": [64, 64, 30, 200],\n    \"warnings\": [],\n    ...\n  },\n  {\n    \"filename\": \"subject02_bold.nii.gz\",\n    \"format\": \"nifti\",\n    \"warnings\": [\"Array is mostly zeros.\"],\n    ...\n  },\n  ...\n]\n</code></pre>"},{"location":"library/batch/#examples","title":"Examples","text":"<pre><code>from voxelkit import report_batch\n\nresults = report_batch(\"data/study_01/\", recursive=True)\n\n# count files with warnings\nflagged = [r for r in results if r.get(\"warnings\")]\nprint(f\"{len(flagged)} of {len(results)} files have warnings\")\n\n# print all warnings\nfor r in flagged:\n    print(r[\"filename\"], \"\u2192\", r[\"warnings\"])\n</code></pre> <p>Save to JSON for later review:</p> <pre><code>import json\nfrom voxelkit import report_batch\n\nresults = report_batch(\"data/study_01/\")\n\nwith open(\"batch_report.json\", \"w\") as f:\n    json.dump(results, f, indent=2)\n</code></pre> <p>Only scan the top level (no subdirectories):</p> <pre><code>results = report_batch(\"data/study_01/\", recursive=False)\n</code></pre> <p>What counts as a supported file?</p> <p>VoxelKit scans for <code>.nii</code>, <code>.nii.gz</code>, <code>.h5</code>, <code>.hdf5</code>, <code>.npy</code>, <code>.npz</code>, <code>.tif</code>, and <code>.tiff</code> files. Other files in the directory are silently skipped.</p>"},{"location":"library/embedding/","title":"Embedding Analysis","text":"<p>Standard QA tools (like <code>report_file</code>) treat your array as a flat collection of numbers. That's fine for most imaging data, but it misses the failure modes that matter for embedding spaces \u2014 things like dead dimensions, collapsed outputs, and outlier sample vectors.</p> <p>These two functions go deeper.</p>"},{"location":"library/embedding/#report_embedding","title":"report_embedding","text":"<pre><code>from voxelkit import report_embedding\n\nreport = report_embedding(\"features.npy\")\nprint(report[\"dead_dim_count\"])    # how many dimensions carry no signal\nprint(report[\"outlier_sample_count\"])  # how many samples are anomalous\n</code></pre>"},{"location":"library/embedding/#signature","title":"Signature","text":"<pre><code>def report_embedding(file_path: str) -&gt; dict\n</code></pre>"},{"location":"library/embedding/#parameters","title":"Parameters","text":"Parameter Type Description <code>file_path</code> <code>str</code> Path to a <code>.npy</code> file containing a 2D <code>(N_samples, D_dims)</code> float array <p>Only .npy is supported</p> <p><code>.npz</code> is not accepted here because embedding matrices are expected to be a single flat array. If your matrix is inside an <code>.npz</code>, extract it first: <code>np.save(\"out.npy\", np.load(\"data.npz\")[\"X\"])</code>.</p>"},{"location":"library/embedding/#return-value","title":"Return value","text":"<pre><code>{\n  \"filename\": \"features.npy\",\n  \"format\": \"numpy\",\n  \"n_samples\": 10000,\n  \"n_dims\": 512,\n  \"dtype\": \"float32\",\n  \"total_nan_count\": 0,\n  \"total_inf_count\": 0,\n  \"dead_dim_count\": 3,\n  \"nan_dim_count\": 0,\n  \"inf_dim_count\": 0,\n  \"norm_mean\": 14.2,\n  \"norm_std\": 1.8,\n  \"outlier_sample_count\": 12,\n  \"warnings\": [\n    \"3/512 dimensions are dead (std \u2248 0). This may indicate a collapsed or undertrained embedding space.\",\n    \"12 sample(s) have anomalous L2 norm (&gt;3\u03c3 from mean). These may be corrupted or out-of-distribution embedding vectors.\"\n  ]\n}\n</code></pre>"},{"location":"library/embedding/#what-gets-checked","title":"What gets checked","text":"<p>Per-dimension (column-wise):</p> <ul> <li>Dead dimensions \u2014 columns whose std across all samples is near zero (<code>&lt; 1e-8</code>). These carry no signal and will silently corrupt downstream distance computations. A warning fires when more than 5% of dimensions are dead.</li> <li>NaN dimensions \u2014 columns with any <code>NaN</code> value. These corrupt any dot product or cosine similarity that uses them.</li> <li>Inf dimensions \u2014 same for <code>Inf</code> values.</li> </ul> <p>Per-sample (row-wise):</p> <ul> <li>Outlier samples \u2014 rows whose L2 norm is more than 3\u03c3 from the mean norm. These are likely corrupted or out-of-distribution embedding vectors.</li> <li>Collapsed embedding space \u2014 if all sample L2 norms are identical (std = 0), the model has collapsed to producing the same output for every input.</li> </ul>"},{"location":"library/embedding/#preview_embedding","title":"preview_embedding","text":"<pre><code>from voxelkit import preview_embedding\n\npng = preview_embedding(\"features.npy\", max_samples=256)\n\nwith open(\"heatmap.png\", \"wb\") as f:\n    f.write(png)\n</code></pre> <p>Renders the embedding matrix as a per-column-normalised heatmap PNG. Each row is one sample, each column is one dimension. Dead dimensions (no variance) appear as uniform mid-grey stripes \u2014 they stand out immediately.</p>"},{"location":"library/embedding/#signature_1","title":"Signature","text":"<pre><code>def preview_embedding(\n    file_path: str,\n    max_samples: int = 256,\n) -&gt; bytes\n</code></pre>"},{"location":"library/embedding/#parameters_1","title":"Parameters","text":"Parameter Type Default Description <code>file_path</code> <code>str</code> \u2014 Path to a <code>.npy</code> 2D embedding file <code>max_samples</code> <code>int</code> <code>256</code> Maximum rows to render. Large matrices are randomly subsampled"},{"location":"library/embedding/#return-value_1","title":"Return value","text":"<p>Raw PNG bytes. Each pixel column is independently normalised so structure within dimensions is visible regardless of overall scale differences.</p>"},{"location":"library/embedding/#full-example","title":"Full example","text":"<pre><code>from voxelkit import report_embedding, preview_embedding\n\n# Check for quality issues\nreport = report_embedding(\"model_outputs.npy\")\n\nprint(f\"Shape: {report['n_samples']} samples \u00d7 {report['n_dims']} dims\")\nprint(f\"Dead dimensions: {report['dead_dim_count']}\")\nprint(f\"Outlier samples: {report['outlier_sample_count']}\")\n\nif report[\"warnings\"]:\n    print(\"\\nWarnings:\")\n    for w in report[\"warnings\"]:\n        print(\" \u2022\", w)\n\n# Visualise it\npng = preview_embedding(\"model_outputs.npy\", max_samples=512)\nwith open(\"heatmap.png\", \"wb\") as f:\n    f.write(png)\n</code></pre>"},{"location":"library/inspect/","title":"inspect_file","text":"<pre><code>from voxelkit import inspect_file\n\nresult = inspect_file(\"scan.nii.gz\")\n</code></pre> <p>Point it at any supported file and you get back a dictionary with the structural facts about that file \u2014 shape, dtype, headers \u2014 without loading the actual voxel/pixel data into memory. Think of it as a fast \"what is this thing?\" tool.</p>"},{"location":"library/inspect/#signature","title":"Signature","text":"<pre><code>def inspect_file(file_path: str | Path) -&gt; dict\n</code></pre>"},{"location":"library/inspect/#parameters","title":"Parameters","text":"Parameter Type Required Description <code>file_path</code> <code>str</code> or <code>Path</code> Yes Path to a <code>.nii</code>, <code>.nii.gz</code>, <code>.h5</code>, <code>.hdf5</code>, <code>.npy</code>, <code>.npz</code>, <code>.tif</code>, or <code>.tiff</code> file"},{"location":"library/inspect/#return-value","title":"Return value","text":"<p>The shape of the returned dictionary depends on the file format.</p> NIfTIHDF5NumPy .npyNumPy .npzTIFF <pre><code>{\n  \"filename\": \"scan.nii.gz\",\n  \"format\": \"nifti\",\n  \"shape\": [64, 64, 30],\n  \"affine\": [[...], ...],\n  \"voxel_sizes\": [3.0, 3.0, 4.0],\n  \"data_dtype\": \"float32\",\n  \"header\": { \"...\": \"...\" }\n}\n</code></pre> <pre><code>{\n  \"filename\": \"data.h5\",\n  \"format\": \"hdf5\",\n  \"datasets\": [\n    {\n      \"path\": \"data/subject01/run1/bold\",\n      \"shape\": [64, 64, 30, 200],\n      \"dtype\": \"float32\"\n    }\n  ]\n}\n</code></pre> <pre><code>{\n  \"filename\": \"features.npy\",\n  \"format\": \"numpy\",\n  \"shape\": [1000, 512],\n  \"dtype\": \"float32\"\n}\n</code></pre> <pre><code>{\n  \"filename\": \"data.npz\",\n  \"format\": \"numpy\",\n  \"arrays\": [\n    { \"name\": \"X\", \"shape\": [500, 256], \"dtype\": \"float64\" },\n    { \"name\": \"y\", \"shape\": [500],      \"dtype\": \"int64\"   }\n  ]\n}\n</code></pre> <pre><code>{\n  \"filename\": \"volume.tiff\",\n  \"format\": \"tiff\",\n  \"shape\": [50, 512, 512],\n  \"ndim\": 3,\n  \"dtype\": \"uint16\",\n  \"page_count\": 50,\n  \"axes\": \"ZYX\"\n}\n</code></pre>"},{"location":"library/inspect/#errors","title":"Errors","text":"Exception When it's raised <code>ValueError</code> The file extension is not supported <code>FileNotFoundError</code> The file path doesn't exist"},{"location":"library/inspect/#example","title":"Example","text":"<pre><code>from voxelkit import inspect_file\n\nresult = inspect_file(\"subject01_bold.nii.gz\")\n\nprint(result[\"shape\"])       # [64, 64, 30, 200]\nprint(result[\"data_dtype\"])  # \"float32\"\nprint(result[\"voxel_sizes\"]) # [3.0, 3.0, 4.0]\n</code></pre> <p>For HDF5 files, you get a list of all datasets inside:</p> <pre><code>result = inspect_file(\"experiment.h5\")\n\nfor ds in result[\"datasets\"]:\n    print(ds[\"path\"], ds[\"shape\"])\n# data/subject01/run1/bold  [64, 64, 30, 200]\n# data/subject02/run1/bold  [64, 64, 30, 198]\n</code></pre>"},{"location":"library/preview/","title":"preview_file","text":"<pre><code>from voxelkit import preview_file\n\npng_bytes = preview_file(\"scan.nii.gz\", plane=\"axial\", slice_index=10)\n\nwith open(\"preview.png\", \"wb\") as f:\n    f.write(png_bytes)\n</code></pre> <p><code>preview_file</code> extracts a 2D slice from your file and returns it as raw PNG bytes. The center slice is used by default if you don't specify one. The image is grayscale, normalised to 0\u2013255.</p>"},{"location":"library/preview/#signature","title":"Signature","text":"<pre><code>def preview_file(\n    file_path: str | Path,\n    *,\n    plane: str = \"axial\",\n    dataset_path: str | None = None,\n    array_name: str | None = None,\n    axis: int | None = None,\n    slice_index: int | None = None,\n) -&gt; bytes\n</code></pre>"},{"location":"library/preview/#parameters","title":"Parameters","text":"Parameter Type Default Description <code>file_path</code> <code>str</code> or <code>Path</code> \u2014 Path to a supported file <code>plane</code> <code>str</code> <code>\"axial\"</code> NIfTI only. One of <code>\"axial\"</code>, <code>\"coronal\"</code>, <code>\"sagittal\"</code> <code>dataset_path</code> <code>str</code> or <code>None</code> <code>None</code> HDF5 only. Path to the dataset inside the file, e.g. <code>\"data/subject01/run1/bold\"</code> <code>array_name</code> <code>str</code> or <code>None</code> <code>None</code> NumPy .npz only. Name of the array inside the archive <code>axis</code> <code>int</code> or <code>None</code> <code>None</code> HDF5, NumPy, TIFF. Which axis to slice along (0, 1, or 2). Required for HDF5 3D datasets <code>slice_index</code> <code>int</code> or <code>None</code> <code>None</code> Index of the slice to extract. Defaults to the centre slice <p>Format-specific rules</p> <p>Some parameters are only valid for certain formats \u2014 passing the wrong one raises a <code>ValidationError</code>.</p> <ul> <li><code>plane</code> \u2192 NIfTI only</li> <li><code>dataset_path</code> \u2192 HDF5 only (required for HDF5)</li> <li><code>array_name</code> \u2192 <code>.npz</code> only</li> <li><code>axis</code> \u2192 HDF5 / NumPy / TIFF (required for 3D HDF5 datasets)</li> </ul>"},{"location":"library/preview/#return-value","title":"Return value","text":"<p>Raw PNG image as <code>bytes</code>. Save it to disk, pass it to a web response, or decode it with a library like Pillow.</p>"},{"location":"library/preview/#errors","title":"Errors","text":"Exception When it's raised <code>ValidationError</code> Wrong parameter used for the detected format <code>ValueError</code> File extension not supported"},{"location":"library/preview/#examples","title":"Examples","text":"NIfTIHDF5NumPy .npyNumPy .npzTIFF <pre><code>from voxelkit import preview_file\n\n# axial centre slice (default)\npng = preview_file(\"bold.nii.gz\")\n\n# specific plane and slice\npng = preview_file(\"bold.nii.gz\", plane=\"coronal\", slice_index=15)\n\nwith open(\"preview.png\", \"wb\") as f:\n    f.write(png)\n</code></pre> <pre><code>from voxelkit import preview_file\n\n# dataset_path and axis are both required for 3D HDF5 datasets\npng = preview_file(\n    \"experiment.h5\",\n    dataset_path=\"data/subject01/run1/bold\",\n    axis=2,\n    slice_index=10,\n)\n\nwith open(\"preview.png\", \"wb\") as f:\n    f.write(png)\n</code></pre> <pre><code>from voxelkit import preview_file\n\n# axis defaults to 0\npng = preview_file(\"volume.npy\", axis=0, slice_index=5)\n\nwith open(\"preview.png\", \"wb\") as f:\n    f.write(png)\n</code></pre> <pre><code>from voxelkit import preview_file\n\n# specify which array inside the archive\npng = preview_file(\"data.npz\", array_name=\"X\", axis=0)\n\nwith open(\"preview.png\", \"wb\") as f:\n    f.write(png)\n</code></pre> <pre><code>from voxelkit import preview_file\n\n# 3D z-stack \u2014 pick an axis and slice\npng = preview_file(\"zstack.tiff\", axis=0, slice_index=25)\n\nwith open(\"preview.png\", \"wb\") as f:\n    f.write(png)\n</code></pre>"},{"location":"library/preview/#using-the-bytes-directly-eg-in-a-notebook","title":"Using the bytes directly (e.g. in a notebook)","text":"<pre><code>from io import BytesIO\nfrom PIL import Image\nfrom voxelkit import preview_file\n\npng = preview_file(\"scan.nii.gz\", plane=\"axial\")\nimg = Image.open(BytesIO(png))\nimg.show()\n</code></pre>"},{"location":"library/report/","title":"report_file","text":"<pre><code>from voxelkit import report_file\n\nreport = report_file(\"scan.nii.gz\")\nprint(report[\"warnings\"])  # [] if everything looks clean\n</code></pre> <p><code>report_file</code> runs a quality check on your file and gives you back a dictionary of statistics \u2014 min, max, mean, std, NaN counts, zero fraction \u2014 along with a <code>warnings</code> list that flags anything suspicious. It's the quickest way to answer \"is this data healthy?\"</p>"},{"location":"library/report/#signature","title":"Signature","text":"<pre><code>def report_file(\n    file_path: str | Path,\n    dataset_path: str | None = None,\n    array_name: str | None = None,\n) -&gt; dict\n</code></pre>"},{"location":"library/report/#parameters","title":"Parameters","text":"Parameter Type Default Description <code>file_path</code> <code>str</code> or <code>Path</code> \u2014 Path to a supported file <code>dataset_path</code> <code>str</code> or <code>None</code> <code>None</code> HDF5 only. Dataset path inside the file. If omitted, the first dataset is used <code>array_name</code> <code>str</code> or <code>None</code> <code>None</code> NumPy .npz only. Array name inside the archive"},{"location":"library/report/#return-value","title":"Return value","text":"<p>A dictionary with QA statistics for the file. The <code>warnings</code> key is always present \u2014 it's an empty list if no issues were found.</p> <p>Example (NIfTI):</p> <pre><code>{\n  \"filename\": \"scan.nii.gz\",\n  \"format\": \"nifti\",\n  \"shape\": [64, 64, 30],\n  \"dtype\": \"float32\",\n  \"min\": -1.2,\n  \"max\": 4103.5,\n  \"mean\": 812.3,\n  \"std\": 401.1,\n  \"nan_count\": 0,\n  \"inf_count\": 0,\n  \"zero_fraction\": 0.04,\n  \"warnings\": []\n}\n</code></pre> <p>See QA Warnings for a full list of what can appear in <code>warnings</code>.</p>"},{"location":"library/report/#errors","title":"Errors","text":"Exception When it's raised <code>ValueError</code> File extension not supported <code>ValidationError</code> Wrong parameter for the detected format"},{"location":"library/report/#examples","title":"Examples","text":"NIfTIHDF5NumPy .npyNumPy .npzTIFF <pre><code>from voxelkit import report_file\n\nreport = report_file(\"bold.nii.gz\")\n\nif report[\"warnings\"]:\n    for w in report[\"warnings\"]:\n        print(\"WARNING:\", w)\nelse:\n    print(\"All clear.\")\n</code></pre> <pre><code>from voxelkit import report_file\n\n# target a specific dataset inside the file\nreport = report_file(\n    \"experiment.h5\",\n    dataset_path=\"data/subject01/run1/bold\",\n)\nprint(report[\"nan_count\"])  # 0\n</code></pre> <pre><code>from voxelkit import report_file\n\nreport = report_file(\"features.npy\")\nprint(report[\"zero_fraction\"])  # e.g. 0.02\n</code></pre> <pre><code>from voxelkit import report_file\n\nreport = report_file(\"data.npz\", array_name=\"X\")\nprint(report[\"warnings\"])\n</code></pre> <pre><code>from voxelkit import report_file\n\nreport = report_file(\"volume.tiff\")\nprint(report[\"shape\"], report[\"dtype\"])\n</code></pre>"}]}